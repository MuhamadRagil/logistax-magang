{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "export PUPPETEER_EXECUTABLE_PATH=\"${PUPPETEER_EXECUTABLE_PATH:-$(command -v chromium)}\" BROWSERSHOT_NODE_BINARY=\"${BROWSERSHOT_NODE_BINARY:-$(command -v node)}\" BROWSERSHOT_NPM_BINARY=\"${BROWSERSHOT_NPM_BINARY:-$(command -v npm)}\" && php artisan migrate --force && php artisan storage:link --force && node /assets/scripts/prestart.mjs /assets/nginx.template.conf /nginx.conf && (php-fpm -y /assets/php-fpm.conf & nginx -c /nginx.conf)",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
